from setuptools import setup

setup(
    name='video_processor',
    packages=['video_processor'],
    include_package_data=True,
    install_requires=[
        'redis'
    ],
    setup_requires=[
    ],
    tests_require=[
    ],
)
